program ejemplo;
var m: integer;
z: boolean;

function f(n, k: integer): integer;
  var p, q:integer;
  begin
    if n<2
      then begin f:=n; p:=0 end
      else
        begin
          f:= f(n-1, p) + f(n-2, q);
          p:= p+q+1
        end
    {write(n, k)}
  end;

begin
    z:=True;
    write(f(3, m) / 2);
    write(z);
    read(m)
end.